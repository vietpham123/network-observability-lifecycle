[defaults]
inventory = inventory/hosts.yml
roles_path = roles
host_key_checking = False
retry_files_enabled = False
result_format = yaml
# ships compliance JSON callbacks; swap for your own log-shipping callback if preferred
